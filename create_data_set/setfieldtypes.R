#Utility file: The "setfieldtypes" utility is used in order to cast these 
#column types explicitly, thereby avoiding auto assignment of "integer64"
#as the column data type. Used by the script **S4_pre_esrd_full.R**

myfieldtypes = c(
  usrds_id = 'integer',
  stay_neo_ip = 'numeric',
  stay_smo_ip = 'numeric',
  stay_alc_ip = 'numeric',
  stay_drg_ip = 'numeric',
  stay_pne_ip = 'numeric',
  stay_kid_ip = 'numeric',
  stay_dia_ip = 'numeric',
  stay_hyp_ip = 'numeric',
  stay_flr_ip = 'numeric',
  stay_cad_ip = 'numeric',
  stay_cvd_ip = 'numeric',
  stay_pad_ip = 'numeric',
  clms_neo_ip = 'numeric',
  clms_smo_ip = 'numeric',
  clms_alc_ip = 'numeric',
  clms_drg_ip = 'numeric',
  clms_pne_ip = 'numeric',
  clms_kid_ip = 'numeric',
  clms_dia_ip = 'numeric',
  clms_hyp_ip = 'numeric',
  clms_flr_ip = 'numeric',
  clms_cad_ip = 'numeric',
  clms_cvd_ip = 'numeric',
  clms_pad_ip = 'numeric',
  # has_neo_ip = 'integer',
  # has_smo_ip = 'integer',
  # has_alc_ip = 'integer',
  # has_drg_ip = 'integer',
  # has_pne_ip = 'integer',
  # has_kid_ip = 'integer',
  # has_dia_ip = 'integer',
  # has_hyp_ip = 'integer',
  # has_flr_ip = 'integer',
  # has_cad_ip = 'integer',
  # has_cvd_ip = 'integer',
  # has_pad_ip = 'integer',
  stay_neo_op = 'numeric',
  stay_smo_op = 'numeric',
  stay_alc_op = 'numeric',
  stay_drg_op = 'numeric',
  stay_pne_op = 'numeric',
  stay_kid_op = 'numeric',
  stay_dia_op = 'numeric',
  stay_hyp_op = 'numeric',
  stay_flr_op = 'numeric',
  stay_cad_op = 'numeric',
  stay_cvd_op = 'numeric',
  stay_pad_op = 'numeric',
  clms_neo_op = 'numeric',
  clms_smo_op = 'numeric',
  clms_alc_op = 'numeric',
  clms_drg_op = 'numeric',
  clms_pne_op = 'numeric',
  clms_kid_op = 'numeric',
  clms_dia_op = 'numeric',
  clms_hyp_op = 'numeric',
  clms_flr_op = 'numeric',
  clms_cad_op = 'numeric',
  clms_cvd_op = 'numeric',
  clms_pad_op = 'numeric',
  # has_neo_op = 'integer',
  # has_smo_op = 'integer',
  # has_alc_op = 'integer',
  # has_drg_op = 'integer',
  # has_pne_op = 'integer',
  # has_kid_op = 'integer',
  # has_dia_op = 'integer',
  # has_hyp_op = 'integer',
  # has_flr_op = 'integer',
  # has_cad_op = 'integer',
  # has_cvd_op = 'integer',
  # has_pad_op = 'integer',
  stay_neo_sn = 'numeric',
  stay_smo_sn = 'numeric',
  stay_alc_sn = 'numeric',
  stay_drg_sn = 'numeric',
  stay_pne_sn = 'numeric',
  stay_kid_sn = 'numeric',
  stay_dia_sn = 'numeric',
  stay_hyp_sn = 'numeric',
  stay_flr_sn = 'numeric',
  stay_cad_sn = 'numeric',
  stay_cvd_sn = 'numeric',
  stay_pad_sn = 'numeric',
  clms_neo_sn = 'numeric',
  clms_smo_sn = 'numeric',
  clms_alc_sn = 'numeric',
  clms_drg_sn = 'numeric',
  clms_pne_sn = 'numeric',
  clms_kid_sn = 'numeric',
  clms_dia_sn = 'numeric',
  clms_hyp_sn = 'numeric',
  clms_flr_sn = 'numeric',
  clms_cad_sn = 'numeric',
  clms_cvd_sn = 'numeric',
  clms_pad_sn = 'numeric',
  # has_neo_sn = 'integer',
  # has_smo_sn = 'integer',
  # has_alc_sn = 'integer',
  # has_drg_sn = 'integer',
  # has_pne_sn = 'integer',
  # has_kid_sn = 'integer',
  # has_dia_sn = 'integer',
  # has_hyp_sn = 'integer',
  # has_flr_sn = 'integer',
  # has_cad_sn = 'integer',
  # has_cvd_sn = 'integer',
  # has_pad_sn = 'integer',
  stay_ip = 'numeric',
  range_ip = 'numeric',
  claims_ip = 'numeric',
  stay_op = 'numeric',
  range_op = 'numeric',
  claims_op = 'numeric',
  stay_sn = 'numeric',
  range_sn = 'numeric',
  claims_sn = 'numeric',
  stay_hh = 'numeric',
  range_hh = 'numeric',
  claims_hh = 'numeric',
  stay_hs = 'numeric',
  range_hs = 'numeric',
  claims_hs = 'numeric',
  claims_range = 'numeric',
  has_neo = 'integer',
  has_smo = 'integer',
  has_alc = 'integer',
  has_drg = 'integer',
  has_pne = 'integer',
  has_kid = 'integer',
  has_dia = 'integer',
  has_hyp = 'integer',
  has_flr = 'integer',
  has_cad = 'integer',
  has_cvd = 'integer',
  has_pad = 'integer',
  prior_hh_care =  'integer',
  prior_hs_care =  'integer',
  prior_ip_care =  'integer',
  prior_op_care =  'integer',
  prior_sn_care =  'integer',
  has_preesrd_claim = 'integer'
)

medevid_fieldtypes = c(
  CDTYPE = "c",
  masked_UREADT = "c",
  ALGCON = "c",
  PATNOTINFORMEDREASON = "c",
  RACEC = "c",
  RACE_SUB_CODE = "c"
)
